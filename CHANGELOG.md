# 更新日志 · 时间规划 TimePlanner

这个文件是**唯一的更新日志**：改动先记在这里，再改版本号，最后发版
（发版步骤见仓库根的 `AGENTS.md` 和 `TimePlanner/release.ps1`）。

## 两条产品线，两个版本序列

| 线 | 界面 | 版本序列 | 标签 | 交付物 |
| --- | --- | --- | --- | --- |
| **特别版**（`special`） | 圣旨皮肤：明黄绢面 + 木牍 + 小人（1.6 起） | 1.6 → 1.7 → sp1 → sp2 … | `special/sp<n>` | `TimePlanner-sp<n>-special` |
| **经典版**（`classic`） | 1.5 那套深色卡片，没有圣旨皮肤、没有小人 | 1.5 → 1.5.1 … | `classic/v<版本>` | `TimePlanner-<版本>-classic` |

两条线的版本号**各走各的**：特别版从 1.6 起另开一条序列（1.6、1.7 是这条序列的头两个版本，
1.8 起改用 `sp<n>` 编号 —— **1.8 就是 `sp1`**，往后 `sp2`、`sp3` …），
经典版接着 1.5 往下走（1.5.1、1.5.2 …），互不覆盖、互不比大小。标签也是分开的两串：
`special/v1.6`、`special/v1.7`、`special/sp1`、`classic/v1.5`（`v1.5` 是它以前的名字，还指着同一个提交）。

**改动往哪儿落（2026-09-23 起）**：这台电脑上的改动**先在经典版生效** —— 改完就
`sync-classic.ps1`（同步内核 + `[需要移植]` 的界面改动 + 编译验证）；**特别版由用户统一点名才生效**，
没说就别改它的版本号、别跑 `release.ps1` 打包。

两条线共用同一份数据 `%APPDATA%\TimePlanner\data.json`（数据结构从 1.5 起没有变过），
但**运行完全隔离**：各自的单实例、进程间信号灯、桌面插件、开机自启条目互不干扰，
两个版本可以同时装着、同时开着，插件各管各的。

## 条目怎么读

- `[内核]`：两条线通用。**经典版可以照单全收** —— 这类改动只落在下面列出的内核文件里，
  同步脚本会把它们整份拷到经典版源码树上再编译。
- `[特别版]` / `[经典版]`：只影响一条线，另一条不用管（多半是皮肤、界面、小人这类）。
- `[需要移植]`：改动落在界面文件里（两条线各写各的界面），`sync-classic.ps1` 会按文字替换
  自动搬过去。哪天搬不动了（编译报错），就手工搬一次，再把搬法补进脚本第 3.5 节。
  个别改动是**整块**落到经典版界面里的（项目页、示例数据这种），逐行替换会越搬越对不上：
  这类改成整段搬迁 —— 搬过去的块尾巴上带一行 `// [sync-classic] 名字：指纹` 注释，
  指纹就是那块内容算出来的；指纹对得上就跳过，内容一变（哪怕只是改了提示语）就整段重铺。
  所以那几块**别在经典版源码里手改**，改请改主线（`work/classic` 那棵树反正也不提交）。

### 内核文件（`[内核]` 条目只改这些）

```
TimePlanner/src/Core/Models.cs          数据结构（改这里 = 数据格式变了，两条线必须一起升）
TimePlanner/src/Core/Store.cs           读写数据、.bak 备份、过期顺延
TimePlanner/src/Core/Projects.cs        项目树（大项目 / 分段 / 小项目）与载入时的自检
TimePlanner/src/Core/Install.cs         版本隔离：互斥体 / 信号灯 / 自启条目的名字
TimePlanner/src/Core/AppSignal.cs       进程间信号灯（把缩在托盘里的窗口叫出来）
TimePlanner/src/Core/DesktopInterop.cs  Win32：窗口层级、找窗口、开机自启
TimePlanner/src/Core/Diagnostics.cs     日志
```

另有一份两条线共用的自绘控件（新文件，`sync-classic.ps1` 整份拷，不做文字替换）：

```
TimePlanner/src/Core/Steps.cs           大项目份数横条 + 份数步进器（sp1 起）
```

同步到经典版（仓库根目录跑）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File sync-classic.ps1                            # 同步 + 编译验证
powershell -NoProfile -ExecutionPolicy Bypass -File sync-classic.ps1 -Version 1.5.1 -Release   # 顺便铺 outputs\ 并打包
```

脚本做五件事：① 把上面这些内核文件（外加 `build.ps1` 和共用的 `Steps.cs`）拷到 `work/classic`
（经典版源码树，`classic/v1.5`）；
② 把经典版入口里写死的互斥体名字换成 `Install.*`；③ 把标 `[需要移植]` 的界面改动搬过去
（能逐行替换的就替换，整块的按内容指纹整段重铺）；
④ 用 `-Edition classic` 编译出 `work/classic/TimePlanner/dist/`。跑几遍结果都一样（幂等）。

---

## 未发布

### 特别版 sp1（未发布）

- 这条线从这一版起改用 `sp<n>` 编号：**本版就是 `sp1`**（按老编号是 1.8），往后 sp2、sp3 …；
  1.6 / 1.7 保持原名不动。标签 `special/sp1`，交付物 `TimePlanner-sp1-special`。

- `[内核]` **版本隔离**：单实例互斥体、进程间信号灯、开机自启条目都改成
  「版别 + 安装目录」算出的短哈希（新增 `src/Core/Install.cs`）。
  同一个目录重复点 = 还是只开一个（照旧把已经开着的那个叫到前面）；不同版本 / 不同目录
  = 各开各的，不再互相挡住。找「自己人」的进程也按目录认亲（`Install.Siblings`），
  不会再误杀别的版本目录里的插件。
- `[内核]` **数据更稳**：覆盖 `data.json` 前先留一份 `data.json.bak`；主文件读不出来
  （被写坏、被杀毒软件截断）时自动从 `.bak` 恢复，并把坏掉的那份存成
  `data.json.corrupt-<时间戳>` 当现场。
- `[内核]` **`Store.RollOver`**：把某天之前没办完的事整体顺延到指定日期；离屏出图的临时
  数据目录也改成按身份分开（`Install.PreviewDataDir`），两个版本同时出图不打架。
- `[需要移植]` **旧账不再隐形**：今日页顶部横幅「此前尚余 N 事未竟，还压在过去的日期里」
  + 「全部挪到今天」；插件列表顶上同款提示、右键菜单「过期未竟挪到今天」。
- `[特别版]` 设置页在「开机自动启动」下面会提一句：还有哪些版本也注册了自启，
  免得登录时几个版本一起起来。
- `[内核]` **项目档案（大项目 / 分段 / 小项目）**：新增 `src/Core/Projects.cs`（项目树）与
  `ProjectNode`（扁平列表 + `ParentId` 成树）、`TaskItem.ProjectId`；`Store` 多了整套项目操作
  （`AddProject` / `RenameProject` / `DeleteProject` / `MoveProject` / `SetProjectOpen` /
  `SetAllProjectsOpen` / `ToggleProjectOpen`）。**最低一级的「小项目」底下挂的就是一条普通事项**：
  于是它天生出现在今日 / 本周 / 桌面插件里，勾选、拖动改期、顺延、礼花都跟任务走同一条路；
  节点名和那条事项的标题由 `Store` 一起改（`RenameProject` / `Update`），不会两边对不上。
  容器（大项目 / 分段）的进度按子孙小项目的完成数算，大项目自己分的份也算在内（`ProjectTree.Count`）。
  载入时 `ProjectTree.Repair` 自检一次：父节点没了的挂回顶层、小项目缺事项自动补一条、
  底下有下級的小项目改判成分段、指着不存在项目的事项退回成普通任务。
- `[需要移植]` **项目档案页**：侧栏新增「项目档案」（角标 = 未竟的小项目数）。页面上可以新建大项目，
  再往里加小项目或分段（等于是把项目分期）、改名、同级上下挪、删除（点一下直接删、不弹确认，
  连子孙和它们的事项一起删）。树形缩进显示，容器带「已竟 x / y」进度胶囊，
  小项目就是一条可勾选的任务行（勾选有礼花，行上的笔/箭头/垃圾桶跟任务一致）。
  两条线的任务行都多了一枚「归属」小标签（`毕业设计 / 开题阶段`），主程序与插件都显示。
- `[需要移植]` 顺延（`RollOver`）和「今日事明日毕」（`Defer`）会带上项目里的事项；
  「已竟之事」页的「清理旧记录」不会动项目的事项（那些由项目页自己管）。
  出图 / 演示用的示例数据里加了两个示例项目（`LoadDemoProjects`），两条线的截图长得一样。
- `[内核]` **大项目按份数计**：`ProjectNode` 多两个字段 `steps` / `reached`（这个大项目共几份、已办几份），
  `Store` 多 `SetProjectSteps` / `SetProjectReached` / `ToggleProjectDone`，另加 `ProjectTree.IsDone`；
  **份数只有「大项目」那一级能自己分**（`SetProjectSteps` 只认大项目，`⊖ n ⊕` 也只长在大项目行上）；
  分段顶多留一个「办完了」的记号（0 / 1 份），小项目本身就是一条事项、一律按 0 算。
  `ProjectTree.Count` 把一个节点的进度算成「自己分的份 + 底下所有小项目」，
  页头统计走只数小项目的 `CountSubs`。载入自检把份数夹回 0..64，已办份数不超过总份数。
- `[需要移植]` **大项目那一行上直接记进度**：大项目行多一条可拖的份数横条（拖到第几格 = 这个大项目
  办到第几份，拖动时只重画格子、松手才写盘）+ `⊖ n ⊕` 份数步进器（0 = 不分份）—— 份数就是在大项目
  这一级上分的，不再分给分段。横条控件在 `src/Core/Steps.cs`（两条线共用，整份拷）。
- `[需要移植]` **容器行的「完成」勾**（跟任务行上的勾选框一个意思）：点一下整段办完 —— 底下的小项目
  全部勾上（连完成时间一起记）、大项目自己分的份置满，再点一下整段撤销；做完照样放一筒礼花 + 一句
  鼓励语（`CelebrateNode`，跟任务勾选同一个礼花筒）。底下什么都没有的空容器，点一下记成「1 份、已办」
  （撤销时连这条记号一起撤掉），不会再是个点了没反应的按钮。
- `[内核]` / `[需要移植]` 份数那套的三个修（段序同「经典版 1.5.2」，源码已落进主线）：
  ① `PartsStepper` 整块吃掉鼠标事件（原来点 ⊕ 会被外层「展开 / 收起」抢走捕获，份数不动）；
  ② 分段也能自己分份（`SetProjectSteps` 认大项目 + 分段，自检不再把分段份数夹成 1）；
  ③ 容器行的「完成」勾换成任务行那只 `CircleCheck` 并挪到行首，办完时名字跟任务标题一样划掉。
  本版（sp1）的交付物还没重出，等发版时一起走 `release.ps1`。
- `[需要移植]` **改份数 / 拖横条之后这一页得重画**：主程序界面的签名（`Signature`）里补上 `Steps` /
  `Reached`。原先改了份数签名不变，`SyncRefresh` 就当「内容没变」直接返回 —— 于是「分成几份」
  「完成置满」这些改动一个都看不见，按钮看着像坏的。这才是那个「点了没反应」的根。

- `[需要移植]` **桌面插件上直接办「几分之几」**：分了份的大项目 / 分段在插件列表里各占一行（按树上的次序排、
  下級缩进），行上只写「x/n」—— 不摆横条，插件就这么点地方；那枚勾跟任务行是同一个 `CircleCheck`，
  点一下**只办完 1 份**，办满最后一份才放礼花、小人才接一句贺辞；办满之后整行跟办完的任务一样收起来
  （开着「已竟之事」才显示），再点一下 = 撤销重来。份数为 0 的节点不另起一行（它们在插件上本来就是
  一条普通事项）。插件签名（`WidgetWindow.Signature`）里补上份数 / 已办，不然在插件上点掉一份界面不重画。

- `[内核]` / `[需要移植]` **存盘失败看得见了（这次真丢过一回数据）**：写 `data.json` 失败时 `Store` 记下原因、
  当场写进 `timeplanner.log`，主程序侧栏底部亮一条红字「⚠ 存盘失败，改动还在内存里（正在重试）+ 原因」，
  补好之后红字自己消失；每次成功写盘再往 `%APPDATA%\TimePlanner\snapshots\data-<时间戳>.json` 留一份快照，
  只留最近 12 份。修法与措辞同「经典版 1.5.2」那两条（源码已落进主线）。

## 已发布

### 特别版序列

#### 特别版 1.7（2026-09-20）· 标签 `special/v1.7`

- `[内核]` 新增进程间信号灯 `AppSignal`：主程序缩在托盘里时窗口句柄是 0，插件右键
  「打开主程序」按了没反应 —— 现在改用命名事件叫它显形。
- `[内核]` 桌面插件的单实例（第二个实例不再静默消失）。
- `[特别版]` 插件顶上周条重做（一格两行：周几 + 日期，今天描红，底下小圆点是当天进度）；
  主程序侧栏也有小人了，贺辞扩到 34 句臣子口吻；新增「今日事明日毕」；
  侧栏「今日进度」和显隐插件按钮挪到最下面给小人腾位置。
- `[需要移植]` 今日页的「今日事明日毕」按钮。

#### 特别版 1.6（2026-09-20）· 标签 `special/v1.6`

- `[特别版]` 整套界面换成「经典古代圣旨」皮肤：木牍 + 明黄绢面 + 朱砂金线 + 鎏金卷轴；
  插件左下角多了一位清朝小人物，用对话框说贺辞；右键菜单换木色皮肤。
- `[内核]` 无。

### 经典版序列

#### 经典版 1.5.2（2026-09-24）· 标签 `classic/v1.5.2`

- `[内核]` **份数步进器点不动**：`⊖ n ⊕` 长在「点名字 = 展开 / 收起」的那一条里，按下时外层把鼠标捕获
  抢走了 —— 点 ⊕ 于是变成收起了项目，份数一个都不变（看着就是「分了份没效果」）。修法：
  `Steps.cs` 里 `PartsStepper` 整块把鼠标事件吃掉（`e.Handled = true`），跟横条（`StepBar`）一个走法。
- `[内核]` **分段也能自己分份**：`Store.SetProjectSteps` 原来只认大项目，现在「大项目 / 分段」都能分；
  载入自检（`Models.Normalize` / `ProjectTree.Repair`）也不再把手写的分段份数夹成 1 份。
  于是 `⊖ n ⊕` + 横条在大项目行和分段行都长，小项目本身就是一条事项、照旧不分份。
- `[需要移植]` **容器行的「完成」勾改成跟任务行一模一样**：原来是一枚小图标（只有颜色变不变），
  现在换成任务行那只 `CircleCheck`（同一个控件、同一套勾选动画），并且挪到行首（任务行的勾也在左边）；
  办完时项目 / 分段的名字跟任务标题一样划掉、图标转成已办色，礼花照放；点一下整段办完、再点撤销。
- `[需要移植]` **桌面插件上直接办「几分之几」**：分了份的大项目 / 分段在插件列表里各占一行（按树上的次序排、
  下級缩进），行上只写「x/n」—— 不摆横条，插件就这么点地方；那枚勾跟任务行是同一个 `CircleCheck`，
  点一下**只办完 1 份**，办满最后一份才放礼花（经典版没有小人，贺辞挂在礼花筒那颗彩纸上）；
  办满之后整行跟办完的任务一样收起来（开着「已完成」才显示），再点一下 = 撤销重来。
  份数为 0 的节点不另起一行（它们在插件上本来就是一条普通事项）。
  插件签名（`WidgetWindow.Signature`）里补上份数 / 已办，不然在插件上点掉一份界面不重画。
- `[内核]` **存盘失败不再无声无息**（这次真丢过一回数据的根）：写盘失败原来被静默吞掉 ——
  界面照常、盘上是旧的，一重启改动全没。现在 `Store` 把原因记进 `WriteError` 并当场写 `timeplanner.log`
  （同一句只记一次，后台每 900ms 重试时不刷日志）。另外每次成功写盘都往
  `%APPDATA%\TimePlanner\snapshots\data-<时间戳>.json` 留一份快照，只留最近 12 份，
  数据被写坏或被别的程序改乱时还能往回倒。
- `[需要移植]` **那条红字**：主程序侧栏底部（`PaintSideFoot`）写盘失败时多一张红字卡片
  「⚠ 存盘失败，改动还在内存里（正在重试）+ 原因」；界面的 `Signature` 里补上 `Store.WriteError`，
  不然红字要等下次重画才出现 / 消失。
#### 经典版 1.5.1（写于 2026-09-23，随 1.5.2 一起发布，没有单独的包）

- 内核整份跟上：版本隔离（`Install.cs`）、`data.json` 的 `.bak` 备份与坏文件恢复、`Store.RollOver`。
- 界面顺手补上：今日页「此前尚余 N 事未竟，还压在过去的日期里」+「全部挪到今天」；
  插件列表顶端同款提示；插件右键「今日事明日毕」「过期未竟挪到今天」。
  （今日页那个按钮在经典版里叫「未完成顺延到明天」，功能一样。）
- 界面顺手补上：侧栏「项目档案」页（大项目 / 分段 / 小项目，最低一级的小项目就是一条可勾选的
  任务行，和任务排在同一列里）、任务行上的项目「归属」小标签（主程序与插件都有）。
- 界面顺手补上：大项目行上的份数横条（拖几格 = 办到第几份）+ `⊖ n ⊕` 份数步进器 ——
  直接从大项目分出几份，分段和小项目不掺和；以及容器行那枚「完成」勾（整段完成 / 撤销）——
  不写小项目也能记进度，控件走共用的 `Steps.cs`。
- 项目页的删除改成一键直删（原来要点两下确认），连子孙和它们的事项一起删。

#### 经典版 1.5（2026-09-20）· 标签 `classic/v1.5`（旧名 `v1.5`）

- `[内核]` 内存占用优化（托盘图标直接调系统接口、插件走软件渲染、空闲回收工作集）；
  按住任务行拖动改期；拖到列表边缘自动翻页；勾选 / 拖拽后不跳回顶部（滚动位置记忆）。
- `[经典版]` 就是这份深色卡片界面本身：没有圣旨皮肤、没有小人。

### 更早（1.0 – 1.4，两条线的共同祖先）

- 1.0 首个版本：今日 / 本周 / 已完成 / 设置四页 + 桌面插件。
- 1.2 `[内核]` 流畅度：写盘后台合并（0.2 秒内的多次改动只写一次）、拖拽自动翻页。
- 1.3 `[特别版]` 右键菜单自绘皮肤与界面细节。
- 1.4 `[特别版]` 完成任务的礼花筒动画（勾选框举起纸筒、彩纸扇形喷出）。

---

## 发版清单（每次照做）

0. **改动默认先落经典版**：改完就 `sync-classic.ps1`（同步内核 + `[需要移植]` 界面改动 + 编译验证）；
   特别版等用户点名才动 —— 没点名就别改它的版本号、别跑 `release.ps1`。
1. 先写这个文件里对应版本的段落，标好 `[内核]` / `[特别版]` / `[经典版]` / `[需要移植]`。
2. 经典版：跑 `sync-classic.ps1 -Version <版本> -Release`（铺包打包），然后打标签 `classic/v<版本>`。
3. 特别版（用户点名才做）：改 `TimePlanner/version.txt`（`sp1`、`sp2` …），
   跑 `TimePlanner/release.ps1`（编译 + 出图 + 铺 `outputs/TimePlanner-<版本>-special/` + 刷新 `exe/`），
   然后打标签 `special/<版本>`（如 `special/sp1`）。
4. `git add -A` + commit + 推 `main` 和标签，最后在 GitHub 建发行版（附件名用 ASCII）。
   细节见 `AGENTS.md`。

